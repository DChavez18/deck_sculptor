<div class="flex <%= chat.role == 'user' ? 'justify-end' : 'justify-start' %> mb-3">
  <div class="max-w-[80%] px-4 py-2 rounded-xl text-sm leading-relaxed whitespace-pre-wrap
              <%= chat.role == 'user' ? 'bg-blue-600 text-white' : 'bg-slate-700 text-slate-200' %>">
    <%= chat.content %>
  </div>
</div>
